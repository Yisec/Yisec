import { VirtualDOM } from "../d";
/**
 * 处理ys:if命令
 *
 * @param {HTMLElement} parent
 * @param {any} node
 * @param {array} ctxs
 */
export default function handleIf(parent: any, node: any, ctxs: any[], parentVdom: VirtualDOM): void;
